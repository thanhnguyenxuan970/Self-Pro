export async function playCelebration(_totalMinutes: number): Promise<void> {
  // SFX disabled — expo-av removed (incompatible with SDK 56 expo-modules-core).
  // Re-enable by installing a compatible audio library and restoring this function.
}
