import React, { useMemo } from 'react';
import { StyleSheet, Text, View } from 'react-native';
import { AnalyticsDashboard } from '../../analytics/dashboardModel';
import { AppColors, FontFamily, Radii, Spacing } from '../../config/theme';

type Props = { data: AnalyticsDashboard; colors: AppColors; isDark: boolean; language: 'vi' | 'en' };
const copy = (vi: boolean) => vi ? {
  volume: 'KHỐI LƯỢNG', points: 'ĐIỂM', stars: 'SAO', goal: 'NGÀY ĐẠT MỤC TIÊU', chart: 'Điểm theo ngày', thisWeek: 'Tuần này', last: 'Trước đó', consistency: 'ĐỀU ĐẶN', rhythm: 'NHỊP ĐỘ', weekday: 'Theo thứ', hour: 'Theo giờ', composition: 'THÀNH PHẦN', share: 'TỶ LỆ', logs: 'LƯỢT', week: 'tuần này', days: 'ngày đạt mục tiêu', peak: 'đỉnh', goalLine: 'MỤC TIÊU 50', all: 'Tất cả thời gian', days30: '30 ngày', goalNote: (n: number, peak: string, value: number) => `${n} / 7 ngày đạt mục tiêu · đỉnh ${peak}, ${value} điểm`, hourNote: 'đỉnh 20–24h'
} : {
  volume: 'VOLUME', points: 'POINTS', stars: 'STARS', goal: 'DAYS AT GOAL', chart: 'Points per day', thisWeek: 'This week', last: 'Last', consistency: 'CONSISTENCY', rhythm: 'RHYTHM', weekday: 'By weekday', hour: 'By hour', composition: 'COMPOSITION', share: 'SHARE', logs: 'N', week: 'this week', days: 'days at goal', peak: 'peak', goalLine: 'GOAL 50', all: 'All time', days30: '30 days', goalNote: (n: number, peak: string, value: number) => `${n} of 7 days above goal · ${peak} ${value} pts`, hourNote: 'peak 20–24h'
};
const delta = (current: number, previous: number) => current - previous;

export function AnalyticsDashboardView({ data, colors: C, isDark, language }: Props) {
  const s = useMemo(() => styles(C, isDark), [C, isDark]);
  const t = copy(language === 'vi');
  const max = Math.max(50, ...data.bars.flatMap(bar => [bar.current, bar.previous]));
  const peak = data.bars.reduce((best, bar) => bar.current > best.current ? bar : best, data.bars[0]);
  const weekdayMax = Math.max(1, ...data.weekday.map(day => day.value));
  const hourMax = Math.max(1, ...data.hours.map(hour => hour.value));
  const totalLogs = Math.max(1, data.composition.reduce((total, row) => total + row.count, 0));
  const metric = (value: number, previous: number, label: string, suffix = '') => <View style={s.metric} key={label}><Text style={s.metricLabel}>{label}</Text><Text style={s.metricValue}>{value}{suffix}</Text><Text style={[s.delta, delta(value, previous) < 0 && s.deltaBad]}>{delta(value, previous) >= 0 ? '▲' : '▼'}{Math.abs(delta(value, previous))}</Text></View>;
  return <View>
    <Text style={s.section}>{t.volume}</Text>
    <View style={s.metrics}>{metric(data.points, data.previousPoints, t.points)}{metric(data.stars, data.previousStars, t.stars)}{metric(data.daysAtGoal, 0, t.goal, `/${data.possibleDays}`)}</View>
    <View style={s.card}>
      <View style={s.chartHeader}><Text style={s.cardTitle}>{t.chart}</Text><Text style={s.legend}><Text style={s.dotCurrent}>●</Text> {t.thisWeek}  <Text style={s.dotPrevious}>●</Text> {t.last}</Text></View>
      <View style={s.chart}>
        <View style={[s.goalLine, { bottom: `${(50 / max) * 100}%` }]} /><Text style={[s.goalText, { bottom: `${(50 / max) * 100}%` }]}>{t.goalLine}</Text>
        {data.bars.map((bar) => <View style={s.barCol} key={bar.label}><View style={s.barPair}><View style={[s.barPrevious, { height: `${Math.max(2, bar.previous / max * 100)}%` }]} /><View style={[s.barCurrent, { height: `${Math.max(2, bar.current / max * 100)}%` }]} /></View><Text style={s.barLabel}>{bar.label}</Text></View>)}
      </View>
      <View style={s.rule} /><Text style={s.note}>{t.goalNote(data.daysAtGoal, peak?.label ?? '', peak?.current ?? 0)}</Text>
    </View>
    <Text style={s.section}>{t.consistency}</Text>
    <View style={s.card}><View style={s.rings}>{[[data.consistency.week, t.week], [data.consistency.month, t.days30], [data.consistency.all, t.all]].map(([value, label]) => <View style={s.ringItem} key={String(label)}><View style={[s.ring, { borderColor: Number(value) >= 60 ? '#FFD54F' : C.faint }]}><Text style={s.ringValue}>{value}</Text></View><Text style={s.ringLabel}>{label}</Text></View>)}</View></View>
    <Text style={s.section}>{t.rhythm}</Text><Text style={s.sectionHint}>12-week averages</Text>
    <View style={s.card}><Text style={s.cardTitle}>{t.weekday}</Text>{data.weekday.map(day => <View style={s.rhythmRow} key={day.label}><Text style={s.rhythmLabel}>{day.label}</Text><View style={s.rhythmTrack}><View style={[s.rhythmFill, { width: `${day.value / weekdayMax * 100}%`, backgroundColor: day.label === 'Sa' ? C.danger : '#FFD54F' }]} /></View><Text style={s.rhythmValue}>{day.value}</Text></View>)}
      <View style={s.rule} /><View style={s.chartHeader}><Text style={s.cardTitle}>{t.hour}</Text><Text style={s.legend}>{t.hourNote}</Text></View><View style={s.hours}>{data.hours.map(hour => <View style={s.hourCol} key={hour.label}><View style={[s.hourBar, { height: `${Math.max(4, hour.value / hourMax * 100)}%` }]} /><Text style={s.hourLabel}>{hour.label}</Text></View>)}</View>
    </View>
    <Text style={s.section}>{t.composition}</Text><Text style={s.sectionHint}>{totalLogs} {t.logs.toLowerCase()} {t.week}</Text>
    <View style={s.card}><View style={s.compHead}><Text style={s.compName}>{language === 'vi' ? 'THÓI QUEN' : 'HABIT'}</Text><Text style={s.compMeta}>{t.share}   {t.logs}   Δ WK</Text></View>{data.composition.map(row => <View style={s.compRow} key={row.name}><Text style={s.compName} numberOfLines={1}>{row.name}</Text><View style={s.compTrack}><View style={[s.compFill, { width: `${row.count / Math.max(1, data.composition[0]?.count ?? 1) * 100}%` }]} /></View><Text style={s.compCount}>{row.count}</Text><Text style={[s.compDelta, row.count < row.previous && s.deltaBad]}>{row.count - row.previous >= 0 ? '+' : ''}{row.count - row.previous}</Text></View>)}</View>
  </View>;
}

function styles(C: AppColors, isDark: boolean) { const card = isDark ? '#171D18' : C.surface; return StyleSheet.create({
  section: { color: '#FFD54F', fontFamily: FontFamily.bold, fontSize: 12, marginTop: 20, marginBottom: 9, letterSpacing: .3 }, sectionHint: { position: 'absolute', right: Spacing.lg, marginTop: -27, color: C.muted, fontFamily: FontFamily.regular, fontSize: 11 },
  metrics: { backgroundColor: card, borderColor: C.line, borderWidth: 1, borderRadius: Radii.lg, flexDirection: 'row', overflow: 'hidden' }, metric: { flex: 1, padding: 13, borderRightColor: C.line, borderRightWidth: 1 }, metricLabel: { color: C.muted, fontFamily: FontFamily.bold, fontSize: 10 }, metricValue: { color: '#FFD54F', fontFamily: FontFamily.extraBold, fontSize: 26, marginTop: 3 }, delta: { color: C.primary, fontFamily: FontFamily.bold, fontSize: 11 }, deltaBad: { color: C.danger }, card: { backgroundColor: card, borderColor: C.line, borderWidth: 1, borderRadius: Radii.lg, padding: 14, marginBottom: 2 }, cardTitle: { color: C.inkDark, fontFamily: FontFamily.bold, fontSize: 14 }, chartHeader: { alignItems: 'center', flexDirection: 'row', justifyContent: 'space-between' }, legend: { color: C.muted, fontFamily: FontFamily.regular, fontSize: 10 }, dotCurrent: { color: '#FFD54F' }, dotPrevious: { color: C.faint }, chart: { height: 180, marginTop: 14, borderBottomColor: C.line2, borderBottomWidth: 1, flexDirection: 'row', alignItems: 'flex-end', justifyContent: 'space-around', position: 'relative' }, goalLine: { borderTopColor: '#FFD54F', borderTopWidth: 1, borderStyle: 'dashed', left: 0, position: 'absolute', right: 0 }, goalText: { color: '#FFD54F', fontFamily: FontFamily.bold, fontSize: 9, position: 'absolute', right: 0 }, barCol: { alignItems: 'center', flex: 1, height: '100%', justifyContent: 'flex-end' }, barPair: { alignItems: 'flex-end', flexDirection: 'row', height: '100%', justifyContent: 'center', width: '100%' }, barCurrent: { backgroundColor: '#FFD54F', borderTopLeftRadius: 3, borderTopRightRadius: 3, marginLeft: 3, width: 8 }, barPrevious: { backgroundColor: isDark ? '#444A43' : C.faint, borderTopLeftRadius: 3, borderTopRightRadius: 3, width: 8 }, barLabel: { color: C.muted, fontFamily: FontFamily.bold, fontSize: 10, marginTop: 8 }, rule: { backgroundColor: C.line, height: 1, marginTop: 14 }, note: { color: C.ink2, fontFamily: FontFamily.regular, fontSize: 11, lineHeight: 16, marginTop: 10 }, rings: { flexDirection: 'row', justifyContent: 'space-around' }, ringItem: { alignItems: 'center' }, ring: { alignItems: 'center', borderRadius: 30, borderWidth: 6, height: 58, justifyContent: 'center', width: 58 }, ringValue: { color: C.inkDark, fontFamily: FontFamily.extraBold, fontSize: 16 }, ringLabel: { color: C.muted, fontFamily: FontFamily.regular, fontSize: 10, marginTop: 7 }, rhythmRow: { alignItems: 'center', flexDirection: 'row', marginTop: 9 }, rhythmLabel: { color: C.muted, fontFamily: FontFamily.bold, fontSize: 10, width: 28 }, rhythmTrack: { backgroundColor: isDark ? '#222A22' : C.surface3, borderRadius: 5, flex: 1, height: 9 }, rhythmFill: { borderRadius: 5, height: 9 }, rhythmValue: { color: C.inkDark, fontFamily: FontFamily.bold, fontSize: 11, textAlign: 'right', width: 30 }, hours: { alignItems: 'flex-end', flexDirection: 'row', height: 80, justifyContent: 'space-around', marginTop: 8 }, hourCol: { alignItems: 'center', height: '100%', justifyContent: 'flex-end', width: 28 }, hourBar: { backgroundColor: '#FFD54F', borderTopLeftRadius: 2, borderTopRightRadius: 2, width: '100%' }, hourLabel: { color: C.muted, fontFamily: FontFamily.regular, fontSize: 9, marginTop: 5 }, compHead: { flexDirection: 'row', justifyContent: 'space-between', marginBottom: 7 }, compMeta: { color: C.muted, fontFamily: FontFamily.bold, fontSize: 9 }, compRow: { alignItems: 'center', flexDirection: 'row', marginTop: 11 }, compName: { color: C.inkDark, flex: 1, fontFamily: FontFamily.bold, fontSize: 12 }, compTrack: { backgroundColor: isDark ? '#222A22' : C.surface3, borderRadius: 4, height: 6, width: 76 }, compFill: { backgroundColor: '#FFD54F', borderRadius: 4, height: 6 }, compCount: { color: C.inkDark, fontFamily: FontFamily.bold, textAlign: 'right', width: 34 }, compDelta: { color: C.primary, fontFamily: FontFamily.bold, textAlign: 'right', width: 31 },
}); }
