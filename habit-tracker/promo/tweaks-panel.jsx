function TweaksPanel({ accent, setAccent, seconds, duration, seek, scenes, setSceneDuration, motionEditor, setMotionEditor }) {
  const colors = ['#25B36E', '#5A6FE0', '#E0567E', '#E0A93B'];
  return <aside className="tweaks"><h1>Tweaks</h1><label>Motion editor <input type="checkbox" checked={motionEditor} onChange={e => setMotionEditor(e.target.checked)} /></label>{motionEditor && <><label>Timeline <input aria-label="Timeline" type="range" min="0" max={duration} step="0.01" value={seconds} onChange={e => seek(e.target.value)} /></label>{scenes.map(scene => <label key={scene.id}>{scene.id} <input aria-label={`${scene.id} duration`} type="number" min="0.1" step="0.1" value={scene.duration} onChange={e => setSceneDuration(scene.id, e.target.value)} /></label>)}</>}<label>Accent <span className="swatches">{colors.map(color => <button key={color} aria-label={`Accent ${color}`} aria-pressed={color === accent} className={`swatch ${color === accent ? 'active' : ''}`} style={{background:color}} onClick={() => setAccent(color)} />)}</span></label>{motionEditor && <div className="time">{seconds.toFixed(2)}s / {duration.toFixed(2)}s</div>}</aside>;
}
